cmake_minimum_required(VERSION 3.8)
project(erc_bringup)
find_package(ament_cmake REQUIRED)
find_package(gz-sim8 REQUIRED)
add_library(erc-grasp-stabilizer-system SHARED src/grasp_stabilizer.cpp)
target_link_libraries(erc-grasp-stabilizer-system gz-sim8::gz-sim8)
install(TARGETS erc-grasp-stabilizer-system LIBRARY DESTINATION lib)
install(DIRECTORY launch config rviz DESTINATION share/${PROJECT_NAME})
ament_package()
